import { RulesetDefinition } from '@api-commons/spotlight-core';

export default {
  extends: [],
  parserOptions: {
    incompatibleValues: 'off',
    duplicateKeys: 'warn',
  },
} as RulesetDefinition;
